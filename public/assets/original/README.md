This directory stores original assets migrated from the current public website.

Run this from the project root to fetch research images from the old site:

```bash
bash scripts/pull-original-images.sh
```

The research page expects the downloaded files under `public/assets/original/research/`.
